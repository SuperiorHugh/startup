when buying use a negative emojis value

moveable purchase menu
- buttons make calls


make new socketPlayer() class
- if moving === true
 - jumping anim
- x, y = serverplayerid.x, serverplayerid.y (potentially an interpolation system)


when new server sends new player event:
- create new socketPlayer
- socketplayer id = server player id
- any data change to server player id will be sent to socketplayer id

    from server
movement message:
{id: PLAYERID, x: PLAYERX, y: PLAYERY, moving: PLAYERMOVING}
emote message:
{id: PLAYERID, emote: PLAYEREMOTE}
connection message:
[
    {id: PLAYERID, x: PLAYERX, y: PLAYERY, moving: PLAYERMOVING},
    {id: PLAYERID, x: PLAYERX, y: PLAYERY, moving: PLAYERMOVING},
    etc...
]



messages will be shared between server and clients (ie: wont be different)

when connecting all prexisting players will be sent to client
